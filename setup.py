from setuptools import setup

setup(
    name="rl_tracking",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'numpy',
                      'matplotlib',
                      'torch']
)
